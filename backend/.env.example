# Copy to .env:  cp .env.example .env
# Points at the Postgres started by `docker compose up -d` (repo root).
# Port 5433, not 5432, so it never clashes with a Postgres you already run.
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/stackanomics
